#include<bits/stdc++.h>
using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    char in[100];
    while(cin.getline(in,100)){
        cout<<in<<"\n";
    }
    return 0;
}